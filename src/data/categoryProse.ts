// Hand-written prose for each category landing page ([category].astro).
// Rendered as raw HTML via set:html — keep markup to <p>, <code>, <ul>/<li>,
// and <strong>. Each entry is written for its specific category; nothing
// here is templated or noun-swapped from another entry.

import type { Category } from './tools';

export const CATEGORY_PROSE: Record<Category, string> = {
  json: `
    <p>JSON won by being boring: no schemas to agree on up front, no whitespace rules to argue about, and a grammar small enough to fit on an index card. That simplicity is also where most of the pain in working with it comes from. A trailing comma, a single-quoted string, an unescaped newline inside a value — any of these produces a document that looks fine to a human and fails <code>JSON.parse</code> outright, usually with an error message that names a byte offset instead of the actual problem.</p>
    <p>The tools in this category exist for the different stages of that problem. <strong>Formatter</strong> and <strong>Minifier</strong> are opposite ends of the same transform — indent for a human to read, strip for a wire payload — and neither changes the data, only its presentation. <strong>Validator</strong> and <strong>Parser</strong> both catch malformed input, but answer different questions: Validator checks conformance and reports where it broke, while Parser is for when you actually want the parsed structure back along with a plain-English explanation of what went wrong. <strong>Viewer</strong> and <strong>JSONPath Tester</strong> are for exploration — collapsing a large document into a navigable tree, or querying it directly instead of scrolling. <strong>Schema Validator</strong> moves up a level, checking not just that a document is valid JSON but that it matches a shape you've declared. <strong>Diff</strong> answers "what changed" between two versions with dot-notation paths instead of a wall of red and green text.</p>
    <p>All of it runs on the browser's native <code>JSON.parse</code>/<code>JSON.stringify</code>, which means the same strictness you'd hit in production — no trailing commas, no comments, double-quoted keys only. That's deliberate: a formatter that's more lenient than the JSON your actual code will parse just moves the failure downstream. The one practical limit worth knowing is a 5 MB input cap on tools that hold the full parsed structure in memory at once, there to keep the tab responsive rather than to silently truncate your data.</p>
  `,

  yaml: `
    <p>YAML reads better than JSON for anything a human edits by hand — Kubernetes manifests, GitHub Actions workflows, docker-compose files — which is exactly why it's the default for configuration and the source of a specific kind of bug JSON can't produce: whitespace that's structurally significant. Two spaces of indentation instead of four silently changes which key nests under which, and there's no closing brace to catch the mistake.</p>
    <p><strong>Formatter</strong> normalizes indentation (2-space or 4-space) and re-serializes the document, which also has the side effect of catching structural errors that display in a text editor but wouldn't survive a real YAML parser. <strong>Validator</strong> checks a document against the YAML 1.2 spec and reports the exact line and column of the first failure — useful when a manifest fails to apply and the CLI error is unhelpfully generic. <strong>Parser</strong> takes that further, showing you the structure YAML actually resolved to, which matters because YAML's implicit typing (an unquoted <code>yes</code> becomes a boolean, an unquoted <code>1.0</code> becomes a float) occasionally resolves a string you meant literally into a different type entirely — the classic "Norway problem" where a country code <code>NO</code> becomes <code>false</code>.</p>
    <p>These tools are built on <code>js-yaml</code>, which supports the YAML 1.1/1.2 feature set most real-world files use — anchors, aliases, multi-document streams, block and flow styles — but doesn't attempt custom tag resolution or language-specific extensions some YAML libraries add (Ruby's <code>!ruby/object</code>, for instance). If a file was authored against one of those extensions, it will round-trip through this parser as plain scalars rather than fail, which is usually what you want but is worth knowing going in. For the JSON↔YAML direction specifically, see the Converters category — key order is preserved, but YAML-only features like anchors and comments don't survive a trip through JSON, since JSON has no equivalent to reconstruct them from.</p>
  `,

  xml: `
    <p>XML is the format everyone converts away from, which is a little unfair — it's more precise than JSON in ways that matter for the documents that still use it: SOAP APIs, RSS/Atom feeds, Office and OOXML file internals, and a long tail of enterprise systems that predate JSON's dominance. Attributes vs. child elements, namespaces, CDATA sections, and mixed content (text and elements interleaved) are all things XML can express cleanly that JSON has no native representation for at all.</p>
    <p><strong>Formatter</strong> pretty-prints with full namespace, CDATA, and comment support, so declarations like <code>xmlns:soap="..."</code> and embedded <code>&lt;![CDATA[...]]&gt;</code> blocks come through structurally intact rather than mangled. <strong>Validator</strong> checks well-formedness — balanced tags, valid character encoding, proper nesting — and reports the line and column of the first structural break; note that well-formed is not the same claim as valid against a DTD or XSD, which this tool doesn't attempt, since schema validation requires resolving and parsing an external schema document this tool never fetches. <strong>Parser</strong> exposes the parsed tree directly, which is the fastest way to answer "does this document actually have the element I expect" without scrolling through a large payload by eye.</p>
    <p>The parsing layer here is the browser's native <code>DOMParser</code>, which means it's exactly as strict as what a browser would do with the same document — a genuine advantage for catching bugs before they reach production, but it also means documents relying on non-standard entity definitions or a DTD-driven default-attribute mechanism won't get those defaults filled in, since DTD processing is out of scope. When you need to move data between XML and JSON, the attribute-vs-element decision is inherently lossy in one direction (attributes have no JSON equivalent, so they get folded into a convention like an <code>@</code>-prefixed key) — see JSON to XML and XML to JSON in Converters for exactly how that mapping is handled, and don't expect a round trip through both to reproduce byte-identical XML.</p>
  `,

  converters: `
    <p>Every converter in this category solves the same underlying problem — you have data in one structured format and need it in another — but the honest answer to "will this convert perfectly" is different for every pair, because JSON, YAML, XML, and CSV don't have the same expressive power. Converting down to a less expressive format always loses something; converting up from one usually requires inventing a convention to fill the gap.</p>
    <p>JSON↔YAML is the closest to lossless of the group, since both are tree-structured with the same core types (objects/maps, arrays/sequences, strings, numbers, booleans, null) — key order is preserved in both directions, and the main thing that doesn't survive is YAML-only syntax like anchors/aliases or comments, since JSON has no analogous concept to encode them into. JSON↔XML is the lossiest pairing here: XML's attribute/element distinction and mixed content model don't map onto JSON's object/array/scalar model without a convention, so this tool folds attributes into <code>@</code>-prefixed keys and text content into a fixed key name — reasonable, but not the only convention that exists, so a round trip won't always reconstruct the original XML.</p>
    <p>CSV↔JSON is the other shape entirely — CSV is flat and tabular where JSON can nest arbitrarily, so JSON to CSV only works cleanly on an array of flat(ish) objects, and nested objects or arrays inside a row get stringified rather than silently dropped. CSV to JSON adds delimiter detection and a header-row toggle so the common cases (comma vs. semicolon, first row as keys vs. positional) don't require manual configuration. CSV to SQL takes the same input one step further, generating INSERT statements with a table name you choose — useful for one-off data loads, though it doesn't infer a schema or column types beyond what's needed to quote values safely, so pair it with a real <code>CREATE TABLE</code> if you need actual typed columns (the Database category's SQL-to-ORM tools go the other direction, DDL to model, once you have one).</p>
  `,

  generators: `
    <p>Every generator here does the same fundamental thing: look at one JSON sample and infer a typed model in some target language — TypeScript, Zod, Go, Python (dataclasses and Pydantic), Java, Rust, Kotlin, or C#. That's a genuinely useful shortcut for turning an API response you're already looking at into a type you can import, but it comes with a hard limitation worth internalizing before you rely on the output: inference from one sample can only see the types that sample happens to contain.</p>
    <p>If a field is <code>null</code> in your sample but sometimes holds a string in production, every generator here will type it as strictly nullable and never as <code>string | null</code> unless the sample itself shows both. The same goes for arrays — an empty array gives no element type to infer, and a field entirely absent from the sample won't appear as an optional property, because there's no signal in a single document that it's supposed to exist elsewhere. None of the generators solve this by guessing; they generate exactly what the sample supports, which means the single highest-leverage thing you can do is paste a sample that's actually representative — a real response with every field populated, ideally from an edge case (a user with no avatar, an order with no discount) rather than the happiest path.</p>
    <p>Beyond that shared constraint, each target has its own translation choices worth knowing: the Zod generator emits both the schema and the TypeScript type Zod would infer from it, so you get runtime validation and a static type from one input. The Go and Rust generators add the idiomatic tags each language's ecosystem expects — <code>json:"..."</code> struct tags in Go, <code>#[derive(Serialize, Deserialize)]</code> in Rust — rather than a bare struct with no serialization wiring. The Python tools split cleanly by use case: dataclasses for a lightweight typed container with no validation, Pydantic when you want the generated model to actually validate incoming data at runtime, matching the distinction those two libraries have in the Python ecosystem itself.</p>
  `,

  encoding: `
    <p>Base64, URL encoding, and HTML entity encoding solve three different problems that all happen to look similar — turning one string into another — and mixing them up is one of the most common small bugs in web development. None of them are encryption or obfuscation; all three are fully reversible by design, and treating any of them as a security measure is a mistake this category's tools won't protect you from.</p>
    <p><strong>Base64</strong> exists to represent arbitrary binary data (or text with awkward bytes) using only printable ASCII characters — the classic use is embedding an image or a JWT segment inline in text-based formats that can't hold raw binary. The encoder here is Unicode-safe via <code>TextEncoder</code>, which matters because a naive <code>btoa()</code> call in JavaScript throws on non-Latin1 characters; this tool handles UTF-8 input correctly instead of failing on the first emoji or accented character. <strong>URL encoding</strong> (percent-encoding) exists for a narrower reason: certain characters are reserved in URLs (<code>?</code>, <code>&amp;</code>, <code>#</code>, space) and need escaping so they're not misread as part of the URL's structure rather than as literal data in a query parameter. <strong>HTML entity encoding</strong> solves a third, unrelated problem — displaying characters that would otherwise be interpreted as markup (<code>&lt;</code>, <code>&gt;</code>, <code>&amp;</code>) safely as visible text in an HTML document.</p>
    <p>The bug these three encodings produce when confused is almost always double-encoding: percent-encoding a string that's already percent-encoded turns <code>%20</code> into <code>%2520</code>, and the same failure mode applies to Base64 and HTML entities. If a round-trip through one of these tools produces a longer, stranger-looking result than you expected, check whether the input was already encoded once before it reached you.</p>
    <p>A related mistake is picking the wrong one for the layer you're actually working at. A value going into a query string needs URL encoding regardless of whether it also happens to be Base64 already (a <code>+</code> or <code>/</code> from Base64 output is itself a reserved URL character and needs escaping on top); text being injected into HTML needs entity encoding regardless of what encoding was used to transport it there. Each tool here does exactly one of these three transforms and nothing else — there's no bundled "encode for the web" mode, because which encodings actually apply depends on where in the pipeline the string is headed, and guessing that wrong is worse than making you choose explicitly.</p>
  `,

  jwt: `
    <p>A JSON Web Token is three Base64URL-encoded segments joined by dots — a header (algorithm and token type), a payload (claims: whatever data the issuer put in), and a signature (proof the first two segments haven't been tampered with, computed with a secret or private key only the issuer holds). The Decoder in this category splits a token into those three parts and shows you the header and payload as readable JSON, along with the expiry claim rendered as a relative time and warnings for two specific red flags: an algorithm of <code>none</code> (a token with no signature at all — historically exploitable when a server accepts it) and a token that's already expired.</p>
    <p>The distinction that matters most here is <strong>decoding is not verifying</strong>. Anyone can Base64-decode a JWT's header and payload without any key at all — that's by design, since JWTs are meant to be inspectable, not confidential. What decoding does <em>not</em> tell you is whether the signature is actually valid for the claimed algorithm and the actual signing key, which is the only thing that proves the token wasn't forged or modified after issuance. This tool decodes for inspection and debugging — reading what a token claims, checking whether it's expired, confirming which algorithm it says it uses — not for authenticating a token as trustworthy. A backend that trusts a JWT's payload without verifying its signature server-side, using the real secret, has a hole an attacker can drive through by handing it a token with a modified payload and a signature that was never checked.</p>
    <p>A second, related trap is where verification is allowed to happen at all. Client-side JavaScript can decode a JWT freely, but it should never be the thing deciding whether that token is trusted — a browser is a hostile execution environment from the server's point of view, and any check performed there can be skipped by an attacker calling your API directly. Signature verification belongs on the server, or in a client that holds the actual public key for an asymmetric algorithm like RS256 and treats it purely as a local sanity check, not as authorization. This category has exactly one tool for exactly that reason: decoding and inspection is the safe, generally useful operation to expose in a browser tool; signature verification against a secret you'd have to paste in is a much narrower, higher-risk workflow that doesn't belong in a shared client-side utility.</p>
  `,

  api: `
    <p>This category covers two related but distinct workflows: turning a single cURL command into request code for a specific language, and converting between the collection formats (Postman, Insomnia, OpenAPI) that teams use to organize and share whole sets of API requests.</p>
    <p>The cURL converters — to Python, Go, Node.js, PHP, Rust, and a Postman collection — all share one parser that reads a cURL command's method, URL, headers, body, form fields, and basic auth, then hand that parsed structure to a per-language emitter. That split matters for correctness: the parsing logic (and its edge cases — quoted arguments, <code>-d</code> vs. <code>--data-raw</code>, multipart <code>-F</code> fields) is written and tested once, not reimplemented six times with six chances to disagree on what a flag means. Every emitter passes the request body through as a raw string rather than re-parsing it as a language-native object, which mirrors what cURL itself does — byte passthrough — and avoids an entire class of "reconstructed the JSON body wrong" bugs at the cost of slightly less idiomatic output.</p>
    <p>The collection converters — Postman↔OpenAPI, Postman↔Insomnia — move between formats with genuinely different data models, not just different syntax for the same thing. OpenAPI describes an API's shape (paths, schemas, parameters, response types) as documentation-first; a Postman collection describes a set of concrete requests someone actually made, often with environment variables and pre-request scripts that have no OpenAPI equivalent at all. Converting Postman → OpenAPI can only infer a schema from the concrete request/response examples in the collection — it won't recover parameter constraints or descriptions that were never in a Postman request to begin with. Converting the other way, OpenAPI → Postman, generates one concrete request per path/method/example but skips anything OpenAPI expresses that Postman has no field for, like complex <code>oneOf</code>/<code>anyOf</code> schema unions — those get flattened to the closest single-shape approximation rather than a fabricated Postman construct that doesn't exist.</p>
  `,

  devops: `
    <p>These tools all take one artifact from your deployment pipeline — a <code>.env</code> file or a <code>docker-compose.yml</code> — and re-render it in the shape another tool in that same pipeline expects, so you're not hand-translating environment variables or service definitions between formats every time you add one.</p>
    <p>The three <code>.env</code> converters (to docker-compose, to a Kubernetes Secret, to GitHub Actions) all read the same simple <code>KEY=value</code> format but target genuinely different destinations: docker-compose gets a plain <code>environment:</code> block, a Kubernetes Secret gets each value base64-encoded per the Secret manifest spec (not for confidentiality — base64 isn't encryption — but because that's the literal wire format <code>kubectl apply</code> expects), and GitHub Actions gets a sequence of <code>gh secret set</code> commands for bulk-importing values into a repo's encrypted secrets store via the CLI. <strong>docker-compose ↔ docker run</strong> moves in both directions between a compose file's declarative service definitions and the equivalent one-shot CLI invocations — the conversion is honest about what doesn't translate: a service that only has a <code>build:</code> context with no <code>image:</code> can't be run directly (compose builds it; <code>docker run</code> doesn't build anything), <code>depends_on</code> ordering has no <code>docker run</code> equivalent since the command doesn't sequence container startup, and <code>deploy.replicas</code> beyond 1 requires running the command in a loop with distinct names, which the output calls out explicitly rather than silently producing one container and calling it done.</p>
    <p>One thing worth saying plainly: these tools process everything client-side and nothing is uploaded, which matters more here than in most categories, since <code>.env</code> files routinely contain real API keys and database credentials. That's exactly why running secrets through any web tool — including this one — is worth being deliberate about; verify a tool doesn't phone home before pasting production credentials into it, and this page's network tab is the fastest way to check.</p>
  `,

  database: `
    <p>Every tool in this category does the same conversion — a SQL <code>CREATE TABLE</code> statement in, a typed ORM model out — for five of the most widely used ORMs across the JavaScript/TypeScript and Python ecosystems. The parsing is shared: one SQL DDL parser extracts table and column structure (name, type, nullability, primary key, uniqueness, auto-increment, default expression, foreign key reference) once, and each target ORM has its own emitter that maps that structure onto its own syntax and conventions. That split is why all five tools handle edge cases the same way — an unmapped <code>DEFAULT</code> expression or a foreign key reference always surfaces as an explicit comment rather than a guess, regardless of which ORM you're generating for.</p>
    <p>The five aren't interchangeable, and picking one is usually already decided by your stack — but here's how they actually differ:</p>
    <ul class="list-disc space-y-1 pl-5">
      <li><strong>Prisma</strong> — TypeScript/JavaScript. Schema-first: you write (or generate) a <code>schema.prisma</code> file, and Prisma's own migration engine generates SQL migrations from it. Strongest type safety of the group via a fully generated client, but the schema DSL is Prisma-specific — you're not writing plain TypeScript to define models.</li>
      <li><strong>Drizzle</strong> — TypeScript. Code-first and deliberately close to raw SQL: models are plain TypeScript objects, and Drizzle's query builder reads a lot like SQL itself with full type inference. Newer than the other four, with a smaller migration tool (<code>drizzle-kit</code>) and ecosystem, but the least "magic" of the TypeScript options if you want to reason about the actual queries being run.</li>
      <li><strong>TypeORM</strong> — TypeScript. Decorator-based (<code>@Entity</code>, <code>@Column</code>) and the most traditional "Active Record or Data Mapper" ORM in this list, closer in spirit to Hibernate or Doctrine than to Prisma or Drizzle. Mature and widely used in NestJS projects specifically, though its decorator-heavy style requires <code>experimentalDecorators</code> and has more implicit behavior (lazy loading, cascade rules) to learn than the code-first options.</li>
      <li><strong>SQLAlchemy</strong> — Python. The most mature and most flexible tool here by a wide margin, with two distinct APIs (Core, for hand-built SQL expressions, and the declarative ORM this generator targets). Nothing else in this list has SQLAlchemy's depth for complex queries, but that flexibility comes with a steeper learning curve than Django's ORM or a simpler dataclass-based approach.</li>
      <li><strong>Sequelize</strong> — JavaScript/TypeScript (Node). Older and more callback/Promise-era in its API design (<code>sequelize.define</code>) than Prisma or Drizzle, still widely deployed in existing Node codebases but rarely the choice for a new project today given the newer alternatives' better TypeScript inference.</li>
    </ul>
    <p>Whichever target you generate for, treat the output as a first draft: foreign keys come through as a comment noting the referenced table and column, not a fully wired relation, since the relation's field name and cardinality aren't determined by the DDL alone — see the SQL to Prisma page's documentation for a worked example of exactly what is and isn't translated automatically.</p>
  `,
};
